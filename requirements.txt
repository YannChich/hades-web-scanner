# WebScan dependencies
# Install: pip install -r requirements.txt
# After install, also run: playwright install chromium

# HTTP client
httpx>=0.27.0

# Terminal UI
rich>=13.7.0

# DNS queries
dnspython>=2.6.1

# WHOIS
python-whois>=0.9.4

# SSL / TLS certificate parsing
cryptography>=42.0.0

# HTML parsing
beautifulsoup4>=4.12.3
lxml>=5.2.0

# Browser automation (screenshots)
playwright>=1.44.0

# PDF report generation
weasyprint>=62.3

# Logging
loguru>=0.7.2

# Favicon hashing
mmh3>=4.1.0

# Port scanning (used by scanner/recon/port_scan.py)
python-nmap>=0.7.1

# Markdown -> HTML for the RedTeam-Tools reference PDF (docs/make_redteam_tools.py)
markdown>=3.6

# Offensive TLS/SSL handshake audit (scanner/tls/hephaestus_tls.py — tls_scan / menu option 9).
# Optional: the module degrades gracefully with an install hint if it is missing.
sslyze>=6.0

# Test runner (dev) — run: pytest tests/
pytest>=8.0
